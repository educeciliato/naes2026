runtime: python312 

entrypoint: gunicorn -b :$PORT --workers 1 --threads 4 naes2026.wsgi

# Substitua "SEUPROJETO" pelo nome do seu projeto Django (o mesmo nome da pasta que tem o arquivo settings.py)


handlers:

- url: /static

  static_dir: static_gcloud/

- url: /.*

  script: auto


automatic_scaling:

  min_instances: 0

  max_instances: 2

env_variables:

  DJANGO_SETTINGS_MODULE: "naes2026.settings"
  DATABASE_URL: "postgresql://neondb_owner:npg_nzZp8BNihqK5@ep-solitary-unit-acy2l56c-pooler.sa-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"
  # Substitua "SEUPROJETO" pelo nome do seu projeto Django (o mesmo nome da pasta que tem o arquivo settings.py)